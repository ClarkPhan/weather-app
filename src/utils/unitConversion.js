export const celciusToFahrenheit = (c) => Math.round(c * (9 / 5) + 32);
export const fahrenheitToCelcius = (f) => Math.round(((f - 32) * 5) / 9);
export const kmToMile = (km) => Math.round(km / 1.60934);
